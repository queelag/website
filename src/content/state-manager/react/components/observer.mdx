---
title: Observer
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_OBSERVER } from '@/definitions/stack-blitz/state-manager/react/components'

The `Observer` component re-renders whenever any of the values it references from an observable inside its children change.

- Optionally the children can be memoized to prevent unnecessary re-renders.
- Optionally the memoization dependencies can be specified.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/state-manager': 'latest',
    '@aracna/state-manager-react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_OBSERVER}
  template='react'
  title='Observer'
/>
