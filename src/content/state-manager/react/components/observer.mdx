---
title: Observer
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { SB_OBSERVER } from '@/definitions/stack-blitz/state-manager/react/components'

The **Observer** component is a custom component that is designed to be used with React functional components. It is used for observing changes to observable data and re-rendering the component when necessary.

The **Observer** component takes two arguments: a function that returns a React node, and a boolean value that indicates whether the observer should use memoization. The function is executed whenever the observer detects a change to the observable data, and the resulting React node is returned to the component.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/state-manager': 'latest',
    '@aracna/state-manager-react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_OBSERVER}
  template='react'
  title='Observer'
/>
