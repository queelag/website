---
title: useObservable
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_OBSERVABLE } from '@/definitions/stack-blitz/state-manager/react/hooks'

The `useObservable` hook returns an observable object that can be used inside a component, changes to observed properties will trigger a re-render.

- Optionally the keys to observe can be specified.
- Optionally the dependencies can be specified.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/state-manager': 'latest',
    '@aracna/state-manager-react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_OBSERVABLE}
  template='react'
  title='useObservable'
/>
