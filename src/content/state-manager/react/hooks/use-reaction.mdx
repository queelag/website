---
title: useReaction
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_REACTION } from '@/definitions/stack-blitz/state-manager/react/hooks'

The `useReaction` hook runs an effect whenever any of the values it references from an observable change inside the expression.
Optionally the dependencies can be specified.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/state-manager': 'latest',
    '@aracna/state-manager-react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_REACTION}
  template='react'
  title='useReaction'
  console
/>
