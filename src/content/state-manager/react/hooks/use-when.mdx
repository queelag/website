---
title: useWhen
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_WHEN } from '@/definitions/stack-blitz/state-manager/react/hooks'

The `useWhen` hook runs an effect whenever the predicate becomes true.
Optionally the dependencies can be specified.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/state-manager': 'latest',
    '@aracna/state-manager-react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_WHEN}
  template='react'
  title='useWhen'
  console
/>
