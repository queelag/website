---
title: Observer
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_OBSERVER } from '@/definitions/stack-blitz/state-manager/react/hocs'

The `observer` HOC re-renders whenever any of the values it references from an observable inside the render function change.

- Optionally the render function can be memoized to prevent unnecessary re-renders.
- Optionally the memoization dependencies can be specified.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/state-manager': 'latest',
    '@aracna/state-manager-react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_OBSERVER}
  template='react'
  title='observer'
/>
