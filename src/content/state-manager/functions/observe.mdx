---
title: observe
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { SB_OBSERVE } from '@/definitions/stack-blitz/state-manager/functions'

The **observe** function takes two arguments: **target** and **keys**. The purpose of this function is to create an observable object from the **target** object and return it.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/state-manager': 'latest' }}
  files={SB_OBSERVE}
  template='javascript'
  title='observe'
  console={100}
/>
