---
title: autorun
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_AUTORUN } from '@/definitions/stack-blitz/state-manager/functions'

The `autorun` function runs an effect immediately and re-runs it whenever any of the values it references from an observable change.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/state-manager': 'latest' }}
  files={SB_AUTORUN}
  template='javascript'
  title='autorun'
  console={100}
/>
