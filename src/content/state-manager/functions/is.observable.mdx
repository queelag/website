---
title: isObservable
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { SB_IS_OBSERVABLE } from '@/definitions/stack-blitz/state-manager/functions'

The **isObservable** function takes a single argument called **target** which is of type **T extends object**. The purpose of this function is to determine whether an object is an observable or not.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/state-manager': 'latest' }}
  files={SB_IS_OBSERVABLE}
  template='javascript'
  title='isObservable'
  console={100}
/>
