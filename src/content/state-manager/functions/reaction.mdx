---
title: reaction
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_REACTION } from '@/definitions/stack-blitz/state-manager/functions'

The **reaction** function takes two arguments: **expression** and **effect**. The purpose of this function is to observe changes to an observable and run an effect function in response to those changes.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/state-manager': 'latest' }}
  files={SB_REACTION}
  template='javascript'
  title='reaction'
  console={100}
/>
