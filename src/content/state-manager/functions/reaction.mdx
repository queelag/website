---
title: reaction
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_REACTION } from '@/definitions/stack-blitz/state-manager/functions'

The `reaction` function runs an effect whenever any of the values it references from an observable change inside the expression.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/state-manager': 'latest' }}
  files={SB_REACTION}
  template='javascript'
  title='reaction'
  console={100}
/>
