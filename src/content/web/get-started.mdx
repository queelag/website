---
order: -1
title: Get Started
---

import { PackageManagerBlock } from '@/blocks/PackageManagerBlock'

The **@aracna/web** package depends on the **@aracna/core** package as a peer dependency, which is required for it to function.

<PackageManagerBlock client:load install={{ dependencies: ['@aracna/core'], package: '@aracna/web' }} />
