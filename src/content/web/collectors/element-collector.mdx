---
title: ElementCollector
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_ELEMENT_COLLECTOR } from '@/definitions/stack-blitz/web/collectors'

The **ElementCollector** class is a utility class that provides methods for storing and retrieving instances of **BaseElement** subclasses. It uses two static maps, **mapByID** and **mapByUID**, to store elements by their id and uid properties, respectively.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_ELEMENT_COLLECTOR}
  template='javascript'
  title='ElementCollector'
  console={100}
/>
