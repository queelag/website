---
title: Document
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_CREATE_DOCUMENT_ELEMENT } from '@/definitions/stack-blitz/web/utils'

This is a collection of functions for working with the document.

## Create an element

The `createDocumentElement` function creates an instance of the element for the specified tag isomorphically.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_CREATE_DOCUMENT_ELEMENT}
  template='javascript'
  title='createDocumentElement'
  console={100}
/>
