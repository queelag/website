---
title: String
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_IS_STRING_SVG } from '@/definitions/stack-blitz/web/utils'

This is a collection of functions for working with strings.

## Check if a string is a valid SVG

The `isStringSVG` function checks if a string is a valid SVG.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_IS_STRING_SVG}
  template='javascript'
  title='isStringSVG'
  console={100}
/>
