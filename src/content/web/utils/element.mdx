---
title: Element
---

import { CodeBlock } from '@/blocks/CodeBlock'
import {
  SB_DEFINE_CUSTOM_ELEMENT,
  SB_GET_ELEMENT_STYLE_COMPATIBLE_VALUE,
  SB_IS_ELEMENT_FOCUSED,
  SB_REMOVE_ELEMENT_ATTRIBUTE,
  SB_REMOVE_IMMUTABLE_ELEMENT_ATTRIBUTE,
  SB_SCROLL_ELEMENT_INTO_VIEW,
  SB_SET_ELEMENT_ATTRIBUTE,
  SB_SET_ELEMENT_ATTRIBUTES,
  SB_SET_IMMUTABLE_ELEMENT_ATTRIBUTE,
  SB_SET_IMMUTABLE_ELEMENT_ATTRIBUTES
} from '@/definitions/stack-blitz/web/utils'

This is a collection of functions for working with HTML elements.

## Define a custom element

The `defineCustomeElement` function defines a custom element.
The difference with the native `customElements.define` is that this function does not throw an error if the element is already defined.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_DEFINE_CUSTOM_ELEMENT}
  template='javascript'
  title='defineCustomElement'
  console={100}
/>

## Get a compatible style value

The `getElementStyleCompatibleValue` function returns a compatible value for the `style` attribute of an element.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_GET_ELEMENT_STYLE_COMPATIBLE_VALUE}
  template='javascript'
  title='getElementStyleCompatibleValue'
  console={100}
/>

## Scroll an element into view

The `scrollElementIntoView` function scrolls an element into view.
Optionally the behavior and logical position can be specified.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_SCROLL_ELEMENT_INTO_VIEW}
  template='javascript'
  title='scrollElementIntoView'
/>

## Set an attribute

The `setElementAttribute` function sets an attribute to an element.
The attribute is removed if the value is not a string.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_SET_ELEMENT_ATTRIBUTE}
  template='javascript'
  title='setElementAttribute'
/>

## Set multiple attributes

The `setElementAttributes` function sets multiple attributes to an element.
The attributes with a value that is not a string are removed.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_SET_ELEMENT_ATTRIBUTES}
  template='javascript'
  title='setElementAttributes'
/>

## Set an immutable attribute

The `setImmutableElementAttribute` function sets an immutable attribute to an element.
The attribute is removed if the value is not a string.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_SET_IMMUTABLE_ELEMENT_ATTRIBUTE}
  template='javascript'
  title='setImmutableElementAttribute'
/>

## Set multiple immutable attributes

The `setImmutableElementAttributes` function sets multiple immutable attributes to an element.
The attributes with a value that is not a string are removed.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_SET_IMMUTABLE_ELEMENT_ATTRIBUTES}
  template='javascript'
  title='setImmutableElementAttributes'
/>

## Remove an attribute

The `removeElementAttribute` function removes an attribute from an element.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_REMOVE_ELEMENT_ATTRIBUTE}
  template='javascript'
  title='removeElementAttribute'
/>

## Remove an immutable attribute

The `removeImmutableElementAttribute` function removes an immutable attribute from an element.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_REMOVE_IMMUTABLE_ELEMENT_ATTRIBUTE}
  template='javascript'
  title='removeImmutableElementAttribute'
/>

## Check if an element is focused

The `isElementFocused` function checks if an element is focused.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_IS_ELEMENT_FOCUSED}
  template='javascript'
  title='isElementFocused'
/>
