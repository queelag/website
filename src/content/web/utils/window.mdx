---
title: Window
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_GET_WINDOW_BOUNDING_CLIENT_RECT } from '@/definitions/stack-blitz/web/utils'

This is a collection of functions for working with the window.

## Get the bounding client rect

The `getWindowBoundingClientRect` function returns the bounding client rect of the window.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest', '@aracna/web': 'latest' }}
  files={SB_GET_WINDOW_BOUNDING_CLIENT_RECT}
  template='javascript'
  title='getWindowBoundingClientRect'
  console={100}
/>
