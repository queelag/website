---
title: Avatar
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { AvatarComponentBlock } from '@/blocks/components/data/AvatarComponentBlock.tsx'
import { SB_AVATAR } from '@/definitions/stack-blitz/web/components/data'

The **AvatarElement** is a custom web component that represents an avatar. An avatar in the context of web development and user interfaces is typically a graphical representation of a user. It can be a picture, an icon, or even a text-based representation. Avatars are commonly used in user interfaces where user identification is important, such as in chat applications, forums, social media platforms, and user profiles.

<AvatarComponentBlock client:load />

<CodeBlock
  client:load
  className='mt-6'
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/web': 'latest',
    '@aracna/web-components': 'latest',
    lit: 'latest',
    'lit-html': 'latest'
  }}
  files={SB_AVATAR}
  template='javascript'
  title='Avatar'
/>
