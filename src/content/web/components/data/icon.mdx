---
title: Icon
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { IconComponentBlock } from '@/blocks/components/data/IconComponentBlock.tsx'
import { SB_ICON } from '@/definitions/stack-blitz/web/components/data'

The **IconElement** is a custom web component that is typically used to display a graphical symbol representing a concept or functionality in a user interface. This component can be reused across different parts of a web application. The icon can be sourced from various libraries or custom designed, and can be styled or manipulated using CSS or JavaScript. It's a way to provide a more visually engaging and intuitive user experience.

<IconComponentBlock client:load />

<CodeBlock
  client:load
  className='mt-6'
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/web': 'latest',
    '@aracna/web-components': 'latest',
    dompurify: 'latest',
    lit: 'latest',
    'lit-html': 'latest'
  }}
  files={SB_ICON}
  template='javascript'
  title='Icon'
/>
