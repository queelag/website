---
title: ma
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_MA } from '@/definitions/stack-blitz/core/functions'

The `ma` function stands for `make async`. It takes a function and returns a function that returns a promise.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_MA} template='javascript' title='ma' console={100} />
