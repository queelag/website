---
title: sleep
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_SLEEP } from '@/definitions/stack-blitz/core/functions'

The `sleep` function takes a number of milliseconds and returns a promise that resolves after that many milliseconds.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_SLEEP} template='javascript' title='rvp' console={100} />
