---
title: rc
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_RC } from '@/definitions/stack-blitz/core/functions'

The **rc** function is a ECMAScript utility function that calls a given function and returns a custom value of a specified type. The name **rc** is short for "return custom", which reflects the function's purpose.

The **rc** function is a simple but useful utility function that can be used in situations where you need to call a function and return a custom value of a specified type. By using **rc** instead of manually calling the function and returning the custom value, you can make your code more concise and easier to read.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_RC} template='javascript' title='rc' console={100} />
