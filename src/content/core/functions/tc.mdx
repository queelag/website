---
title: tc
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_TC } from '@/definitions/stack-blitz/core/functions'

The `tc` function stands for `try catch`. It takes a function and returns the result of the function or the error that was thrown.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_TC} template='javascript' title='tc' console={100} />
