---
title: mtcp
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_MTCP } from '@/definitions/stack-blitz/core/functions'

The `mtcp` function stands for `make try catch promise`. It takes an async function and returns a function that will return the result of the original function or an error.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_MTCP} template='javascript' title='mtcp' console={100} />
