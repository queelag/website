---
title: typeahead
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_TYPEAHEAD } from '@/definitions/stack-blitz/core/functions'

The `typeahead` function is used to search for items in a list, based on user input. When a match is found, the `match` event is emitted.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_TYPEAHEAD} template='javascript' title='Typeahead' console={100} />
