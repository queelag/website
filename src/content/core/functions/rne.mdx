---
title: rne
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_RNE } from '@/definitions/stack-blitz/core/functions'

The `rne` function stands for `return new error`. It takes a message and options and returns a new error.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_RNE} template='javascript' title='rne' console={100} />
