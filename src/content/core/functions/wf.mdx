---
title: wf
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_WF } from '@/definitions/stack-blitz/core/functions'

The `wf` function stands for `wait for`. It takes a function and waits for it to return a truthy value or an error.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_WF} template='javascript' title='wf' console={100} />
