---
title: wfp
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_WFP } from '@/definitions/stack-blitz/core/functions'

The `wfp` function stands for `wait for promise`. It takes a function that returns a promise and waits for it to resolve to a truthy value or an error.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_WFP} template='javascript' title='wfp' console={100} />
