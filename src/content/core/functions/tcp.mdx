---
title: tcp
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_TCP } from '@/definitions/stack-blitz/core/functions'

The `tcp` function stands for `try catch promise`. It takes a function and returns a promise that resolves to the result of the function or the error that was thrown.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_TCP} template='javascript' title='tcp' console={100} />
