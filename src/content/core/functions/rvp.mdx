---
title: rvp
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_RVP } from '@/definitions/stack-blitz/core/functions'

The `rvp` function stands for `return void promise`. It takes a function and returns a promise that resolves to nothing.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_RVP} template='javascript' title='rvp' console={100} />
