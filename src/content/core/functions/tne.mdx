---
title: tne
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_TNE } from '@/definitions/stack-blitz/core/functions'

The `tne` function stands for `throw new error`. It takes a message and options and throws a new error.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_TNE} template='javascript' title='tne' console={100} />
