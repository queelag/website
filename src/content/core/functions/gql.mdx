---
title: gql
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_GQL } from '@/definitions/stack-blitz/core/functions'

The `gql` function is a dirty hack to get editor features for GraphQL queries without using the real GraphQL parser.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_GQL} template='javascript' title='gql' console={100} />
