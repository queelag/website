---
title: noop
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_NOOP } from '@/definitions/stack-blitz/core/functions'

The `noop` function stands for `no operation`. It takes any number of arguments and returns nothing.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_NOOP} template='javascript' title='noop' console={100} />
