---
title: Timeout
---

import { Alert } from '@/components/Alert'
import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_TIMEOUT_CLEAR, SB_TIMEOUT_CLEAR_EVERY, SB_TIMEOUT_IS_SET, SB_TIMEOUT_SET, SB_TIMEOUT_SET_CUSTOM_KEY } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with timeouts.

## Set

The `setTimeout` function sets a timeout to run a function after `ms` milliseconds. When calling `setTimeout` with only the function and the time, the function will be used as the key.

<Alert client:load severity='warning'>
  The function passed to the `setInterval` function must be constant, otherwise you will not be able to clear the interval.
</Alert>

<CodeBlock
  client:load
  className='mt-6'
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_TIMEOUT_SET}
  template='javascript'
  title='Timeout'
  console={100}
/>

## Set with a custom key

The `setTimeout` function can also be called with a custom key. This is useful when you want to clear a timeout with a function that is not constant.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_TIMEOUT_SET_CUSTOM_KEY} template='javascript' title='Timeout' console={100} />

## Clear

The `clearTimeout` function clears a timeout.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_TIMEOUT_CLEAR} template='javascript' title='Timeout' console={100} />

## Clear all

The `clearEveryTimeout` function clears all timeouts.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_TIMEOUT_CLEAR_EVERY} template='javascript' title='Timeout' console={100} />

## Check if a timeout is set

The `isTimeoutSet` function checks if a timeout is set.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_TIMEOUT_IS_SET} template='javascript' title='Timeout' console={100} />
