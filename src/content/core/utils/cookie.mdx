---
title: Cookie
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_SERIALIZE_COOKIE, SB_DESERIALIZE_COOKIE } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with HTTP cookies.

## Serialize

The `serializeCookie` function serializes a cookie name-value pair into a string suitable for use in a `Set-Cookie` header or `document.cookie` property.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_SERIALIZE_COOKIE} template='javascript' title='serializeCookie' console={100} />

## Deserialize

The `deserializeCookie` function deserializes a cookie string to an object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_DESERIALIZE_COOKIE}
  template='javascript'
  title='deserializeCookie'
  console={100}
/>
