---
title: Object
---

import { CodeBlock } from '@/blocks/CodeBlock'
import {
  SB_CLONE_OBJECT,
  SB_COPY_OBJECT_PROPERTY,
  SB_DELETE_OBJECT_PROPERTY,
  SB_DELETE_OBJECT_PROPERTIES,
  SB_FLATTEN_OBJECT,
  SB_GET_OBJECT_PROPERTY,
  SB_HAS_OBJECT_PROPERTY,
  SB_IS_OBJECT,
  SB_IS_OBJECT_CLONABLE,
  SB_IS_OBJECT_FLATTENABLE,
  SB_IS_OBJECT_KEYS_POPULATED,
  SB_IS_OBJECT_VALUES_POPULATED,
  SB_IS_PLAIN_OBJECT,
  SB_MERGE_OBJECTS,
  SB_OMIT_OBJECT_PROPERTIES,
  SB_PICK_OBJECT_PROPERTIES,
  SB_SET_OBJECT_PROPERTY
} from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with objects.

## Clone

The `cloneObject` function creates a copy of an object. Optionally the copy can be deep.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_CLONE_OBJECT} template='javascript' title='cloneObject' console={100} />

## Copy a property

The `copyObjectProperty` function copies a property from one object to another. The key supports bracket and dot notation.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_COPY_OBJECT_PROPERTY}
  template='javascript'
  title='copyObjectProperty'
  console={100}
/>

## Delete a property

The `deleteObjectProperty` function deletes a property from an object. The key supports bracket and dot notation.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_DELETE_OBJECT_PROPERTY}
  template='javascript'
  title='deleteObjectProperty'
  console={100}
/>

## Delete properties

The `deleteObjectProperties` function deletes the properties of an object that either match the predicate or are in the list of keys. The keys support bracket and dot notation. Optionally deletes deep properties as well.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_DELETE_OBJECT_PROPERTIES}
  template='javascript'
  title='deleteObjectProperties'
  console={100}
/>

## Flatten

The `flattenObject` function flattens an object into a single-depth object with dot notation keys. Optionally flattens arrays as well.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_FLATTEN_OBJECT} template='javascript' title='flattenObject' console={100} />

## Get a property

The `getObjectProperty` function returns a property from an object. The key supports bracket and dot notation.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_GET_OBJECT_PROPERTY}
  template='javascript'
  title='getObjectProperty'
  console={100}
/>

## Merge

The `mergeObjects` function merges two or more objects into a single object.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_MERGE_OBJECTS} template='javascript' title='mergeObjects' console={100} />

## Omit properties

The `omitObjectProperties` function returns a new object without the properties that match the predicate or are in the list of keys. The keys support bracket and dot notation. Optionally omits deep properties as well.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_OMIT_OBJECT_PROPERTIES}
  template='javascript'
  title='omitObjectProperties'
  console={100}
/>

## Pick properties

The `pickObjectProperties` function returns a new object with only the properties that match the predicate or are in the list of keys. The keys support bracket and dot notation. Optionally picks deep properties as well.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_PICK_OBJECT_PROPERTIES}
  template='javascript'
  title='pickObjectProperties'
  console={100}
/>

## Set a property

The `setObjectProperty` function sets a property on an object. The key supports bracket and dot notation.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_SET_OBJECT_PROPERTY}
  template='javascript'
  title='setObjectProperty'
  console={100}
/>

## Has a property

The `hasObjectProperty` function checks if an object has a property. The key supports bracket and dot notation.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_HAS_OBJECT_PROPERTY}
  template='javascript'
  title='hasObjectProperty'
  console={100}
/>

## Check if a value is an object

The `isObject` function checks if a unknown value is an object. A value is considered an object if it is typeof "object", not null and not an array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_OBJECT} template='javascript' title='isObject' console={100} />

## Check if an object is clonable

The `isObjectClonable` function checks if an object is clonable. An object is considered clonable if it is an array or a plain object.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_OBJECT_CLONABLE} template='javascript' title='isObjectClonable' console={100} />

## Check if an object is flattenable

The `isObjectFlattenable` function checks if an object is flattenable. An object is considered flattenable if it is an array or a plain object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_IS_OBJECT_FLATTENABLE}
  template='javascript'
  title='isObjectFlattenable'
  console={100}
/>

## Check if an object has keys

The `isObjectKeysPopulated` function checks if an object has keys.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_IS_OBJECT_KEYS_POPULATED}
  template='javascript'
  title='isObjectKeysPopulated'
  console={100}
/>

## Check if an object has values

The `isObjectValuesPopulated` function checks if an object has values.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_IS_OBJECT_VALUES_POPULATED}
  template='javascript'
  title='isObjectValuesPopulated'
  console={100}
/>

## Check if a value is a plain object

The `isPlainObject` function checks if a unknown value is a plain object. A value is considered a plain object if it matches the default object prototype, it is typeof "object" and not null.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_PLAIN_OBJECT} template='javascript' title='isPlainObject' console={100} />
