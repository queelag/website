---
title: Promise
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_IS_PROMISE, SB_IS_PROMISE_LIKE } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with promises.

## Check if an unknown value is a Promise

The `isPromise` function checks if an unknown value is a Promise.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_PROMISE} template='javascript' title='isPromise' console={100} />

## Check if an unknown value is PromiseLike

The `isPromiseLike` function checks if an unknown value is PromiseLike.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_PROMISE_LIKE} template='javascript' title='isPromiseLike' console={100} />
