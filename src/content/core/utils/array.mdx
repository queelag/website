---
title: Array
---

import { CodeBlock } from '@/blocks/CodeBlock'
import {
  SB_CLONE_ARRAY,
  SB_GET_ARRAYS_DIFFERENCE,
  SB_GET_ARRAYS_INTERSECTION,
  SB_GET_ARRAY_LAST_ITEM,
  SB_REMOVE_ARRAY_DUPLICATES,
  SB_REMOVE_ARRAY_ITEMS,
  SB_IS_ARRAY
} from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with arrays.

## Create a clone

The `cloneArray` function creates a copy of an array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_CLONE_ARRAY} template='javascript' title='cloneArray' console={100} />

## Get the symmetric difference

The `getArraysDifference` function returns the symmetric difference between two or more arrays. Optionally you can pass a custom `includes` function to check if an item is included in the result array.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_GET_ARRAYS_DIFFERENCE}
  template='javascript'
  title='getArraysDifference'
  console={100}
/>

## Intersect two or more arrays

The `getArraysIntersection` function returns the intersection between two or more arrays. Optionally you can pass a custom `includes` function to check if an item is included in the result array.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_GET_ARRAYS_INTERSECTION}
  template='javascript'
  title='getArraysIntersection'
  console={100}
/>

## Get the last item

The `getArrayLastItem` function returns the last item of an array. Optionally you can pass a fallback value that will be returned if the array is empty.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_GET_ARRAY_LAST_ITEM}
  template='javascript'
  title='getArrayLastItem'
  console={100}
/>

## Remove duplicates

The `removeArrayDuplicates` function removes all duplicates from an array. Optionally you can pass a custom `includes` function to check if an item is included in the result array.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_REMOVE_ARRAY_DUPLICATES}
  template='javascript'
  title='removeArrayDuplicates'
  console={100}
/>

## Remove items

The `removeArrayItems` function removes items from an array that match the predicate or are in the items array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_REMOVE_ARRAY_ITEMS} template='javascript' title='removeArrayItems' console={100} />

## Check if a value is an array

The `isArray` function checks if the given value is an array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_ARRAY} template='javascript' title='isArray' console={100} />
