---
title: URL
---

import { CodeBlock } from '@/blocks/CodeBlock'
import {
  SB_APPEND_SEARCH_PARAMS_TO_URL,
  SB_CONCAT_URL,
  SB_DESERIALIZE_URL_SEARCH_PARAMS,
  SB_REMOVE_SEARCH_PARAMS_FROM_URL,
  SB_SERIALIZE_URL_SEARCH_PARAMS,
  SB_IS_URL
} from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with URLs.

## Append search params

The `appendSearchParamsToURL` function appends the search params to a URL.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_APPEND_SEARCH_PARAMS_TO_URL}
  template='javascript'
  title='appendSearchParamsToURL'
  console={100}
/>

## Remove search params

The `removeSearchParamsFromURL` functions removes the search params from an URL.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_REMOVE_SEARCH_PARAMS_FROM_URL}
  template='javascript'
  title='removeSearchParamsFromURL'
  console={100}
/>

## Concatenate URL pathnames

The `concatURL` function concatenates a URL with pathnames.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_CONCAT_URL} template='javascript' title='concatURL' console={100} />

## Deserialize URL search params

The `deserializeURLSearchParams` function deserializes an array, string, plain object or `URLSearchParams` to an array, string or plain object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_DESERIALIZE_URL_SEARCH_PARAMS}
  template='javascript'
  title='deserializeURLSearchParams'
  console={100}
/>

## Serialize URL search params

The `serializeURLSearchParams` function serializes an array, string or plain object to `URLSearchParams`.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_SERIALIZE_URL_SEARCH_PARAMS}
  template='javascript'
  title='serializeURLSearchParams'
  console={100}
/>

## Check if an unknown value is an URL

The `isURL` function checks if an unknown value is an URL.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_URL} template='javascript' title='isURL' console={100} />
