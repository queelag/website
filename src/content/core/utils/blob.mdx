---
title: Blob
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_SERIALIZE_BLOB, SB_DESERIALIZE_BLOB } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with `Blob` objects.

## Serialize

The `serializeBlob` function creates a new `AracnaBlob` from a `Blob`.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_SERIALIZE_BLOB} template='javascript' title='serializeBlob' console={100} />

## Deserialize

The `deserializeBlob` function creates a new `Blob` from an `AracnaBlob`. Optionally resolves the data of the blob.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_DESERIALIZE_BLOB} template='javascript' title='deserializeBlob' console={100} />
