---
title: Base64
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_BASE64_ENCODE, SB_BASE64_DECODE, SB_BASE64_URL_DECODE, SB_BASE64_URL_ENCODE } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with base64.

## Encode

The `encodeBase64` function encodes an array of integers into a base64 string.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE64_ENCODE} template='javascript' title='Base64' console={100} />

## Decode

The `decodeBase64` function decodes a base64 string into a Uint8Array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE64_DECODE} template='javascript' title='Base64' console={100} />

## Encode URL

The `encodeBase64URL` function encodes an array of integers into a base64 URL string.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE64_URL_ENCODE} template='javascript' title='Base64' console={100} />

## Decode URL

The `decodeBase64URL` function decodes a base64 URL string into a Uint8Array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE64_URL_DECODE} template='javascript' title='Base64' console={100} />
