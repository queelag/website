---
title: FormData
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_SERIALIZE_FORM_DATA, SB_DESERIALIZE_FORM_DATA } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with `FormData` objects.

## Serialize

The `serializeFormData` function serializes an object into a `FormData` object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_SERIALIZE_FORM_DATA}
  template='javascript'
  title='serializeFormData'
  console={100}
/>

## Deserialize

The `deserializeFormData` function deserializes a `FormData` object into an object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_DESERIALIZE_FORM_DATA}
  template='javascript'
  title='deserializeFormData'
  console={100}
/>
