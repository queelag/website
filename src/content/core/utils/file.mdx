---
title: File
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_SERIALIZE_FILE, SB_DESERIALIZE_FILE } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with `File` objects.

## Serialize

The `serializeFile` function creates a new `AracnaFile` from a `File`.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_SERIALIZE_FILE} template='javascript' title='serializeFile' console={100} />

## Deserialize

The `deserializeFile` function creates a new `File` from an `AracnaFile`. Optionally resolves the data of the file.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_DESERIALIZE_FILE} template='javascript' title='deserializeFile' console={100} />
