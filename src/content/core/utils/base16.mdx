---
title: Base16
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_BASE16_ENCODE, SB_BASE16_DECODE } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with base16, also known as hex.

## Encode

The `encodeBase16` function encodes an array of integers into a base16 string.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE16_ENCODE} template='javascript' title='Base16' console={100} />

## Decode

The `decodeBase16` function decodes a base16 string into a Uint8Array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE16_DECODE} template='javascript' title='Base16' console={100} />
