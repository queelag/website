---
title: Fetch
---

import { CodeBlock } from '@/blocks/CodeBlock'
import {
  SB_DELETE_FETCH_REQUEST_INIT_HEADER,
  SB_GET_FETCH_REQUEST_INIT_HEADER,
  SB_GET_FETCH_REQUEST_INIT_HEADERS_ENTRIES,
  SB_COUNT_FETCH_REQUEST_INIT_HEADERS,
  SB_SET_FETCH_REQUEST_INIT_HEADER,
  SB_SET_FETCH_REQUEST_INIT_HEADER_WHEN_UNSET,
  SB_HAS_FETCH_REQUEST_INIT_HEADER,
  SB_MERGE_FETCH_REQUEST_INITS,
  SB_TO_NATIVE_FETCH_REQUEST_INIT,
  SB_TO_LOGGABLE_FETCH_REQUEST_INIT,
  SB_TO_LOGGABLE_NATIVE_FECTH_REQUEST_INIT
} from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with fetch.

## Count a request headers

The `countFetchRequestInitHeaders` function counts the number of headers in a `FetchRequestInit` or `RequestInit` object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_COUNT_FETCH_REQUEST_INIT_HEADERS}
  template='javascript'
  title='countFetchRequestInitHeaders'
  console={100}
/>

## Delete a request header

The `deleteFetchRequestInitHeader` function deletes a header from a `FetchRequestInit` or `RequestInit` object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_DELETE_FETCH_REQUEST_INIT_HEADER}
  template='javascript'
  title='deleteFetchRequestInitHeader'
  console={100}
/>

## Get a request header

The `getFetchRequestInitHeader` function returns a header from a `FetchRequestInit` or `RequestInit` object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_GET_FETCH_REQUEST_INIT_HEADER}
  template='javascript'
  title='getFetchRequestInitHeader'
  console={100}
/>

## Get a request headers entries

The `getFetchRequestInitHeadersEntries` function returns the headers entries of a `FetchRequestInit` or `RequestInit` object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_GET_FETCH_REQUEST_INIT_HEADERS_ENTRIES}
  template='javascript'
  title='getFetchRequestInitHeadersEntries'
  console={100}
/>

## Set a request header

The `setFetchRequestInitHeader` function sets a header in a `FetchRequestInit` or `RequestInit` object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_SET_FETCH_REQUEST_INIT_HEADER}
  template='javascript'
  title='setFetchRequestInitHeader'
  console={100}
/>

## Set a request header when unset

The `setFetchRequestInitHeaderWhenUnset` function sets a header in a `FetchRequestInit` or `RequestInit` object if it is not already set.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_SET_FETCH_REQUEST_INIT_HEADER_WHEN_UNSET}
  template='javascript'
  title='setFetchRequestInitHeaderWhenUnset'
  console={100}
/>

## Check if a request header exists

The `hasFetchRequestInitHeader` function checks if a header exists in a `FetchRequestInit` or `RequestInit` object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_HAS_FETCH_REQUEST_INIT_HEADER}
  template='javascript'
  title='hasFetchRequestInitHeader'
  console={100}
/>

## Merge request inits

The `mergeFetchRequestInits` function merges two or more `FetchRequestInit` or `RequestInit` objects.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_MERGE_FETCH_REQUEST_INITS}
  template='javascript'
  title='mergeFetchRequestInits'
  console={100}
/>

## Convert a request to a native request

The `toNativeFetchRequestInit` function converts a `FetchRequestInit` or `RequestInit` object to a native `RequestInit` object.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_TO_NATIVE_FETCH_REQUEST_INIT}
  template='javascript'
  title='toNativeFetchRequestInit'
  console={100}
/>

## Make a request log friendly

The `toLoggableFetchRequestInit` function returns a version of a `FetchRequestInit` object that is easier to read in logs.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_TO_LOGGABLE_FETCH_REQUEST_INIT}
  template='javascript'
  title='toLoggableFetchRequestInit'
  console={100}
/>

## Make a native request log friendly

The `toLoggableNativeFetchRequestInit` function returns a version of a `RequestInit` object that is easier to read in logs.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_TO_LOGGABLE_NATIVE_FECTH_REQUEST_INIT}
  template='javascript'
  title='toLoggableNativeFetchRequestInit'
  console={100}
/>
