---
title: Function
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_IS_INSTANCE_OF } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with functions.

## Check if a value is an instance of a function

The `isInstanceOf` function checks if a value is an instance of a function.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_INSTANCE_OF} template='javascript' title='isInstanceOf' console={100} />
