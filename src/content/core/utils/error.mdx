---
title: Error
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_IS_ERROR } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with errors.

## Check if a value is an error

The `isError` function checks if a value is an error, whilst the `isNotError` function checks if a value is not an error.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_IS_ERROR} template='javascript' title='isError' console={100} />
