---
title: Base32
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_BASE32_ENCODE, SB_BASE32_DECODE, SB_BASE32_HEX_ENCODE, SB_BASE32_HEX_DECODE } from '@/definitions/stack-blitz/core/utils'

This is a collection of functions for working with base32.

## Encode

The `encodeBase32` function encodes an array of integers into a base32 string.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE32_ENCODE} template='javascript' title='Base32' console={100} />

## Decode

The `decodeBase32` function decodes a base32 string into a Uint8Array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE32_DECODE} template='javascript' title='Base32' console={100} />

## Encode Hex

The `encodeBase32Hex` function encodes an array of integers into a base32hex string.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE32_HEX_ENCODE} template='javascript' title='Base32' console={100} />

## Decode Hex

The `decodeBase32Hex` function decodes a base32hex string into a Uint8Array.

<CodeBlock client:load dependencies={{ '@aracna/core': 'latest' }} files={SB_BASE32_HEX_DECODE} template='javascript' title='Base32' console={100} />
