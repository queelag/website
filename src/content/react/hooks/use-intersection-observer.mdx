---
title: useIntersectionObserver
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_INTERSECTION_OBSERVER } from '@/definitions/stack-blitz/react/hooks'

The `useIntersectionObserver` hook creates an IntersectionObserver and observes the given targets.

- Optionally the root element, root margin and threshold can be configured.
- Optionally a list of dependencies can be passed to refresh the IntersectionObserver.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_INTERSECTION_OBSERVER}
  template='react'
  title='useIntersectionObserver'
/>
