---
title: useDOMRect
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_DOM_RECT } from '@/definitions/stack-blitz/react/hooks'

The `useDOMRect` hook returns the DOMRect of an element.
Optionally a dependency list can be passed to refresh the DOMRect.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_DOM_RECT}
  template='react'
  title='useDOMRect'
  console
/>
