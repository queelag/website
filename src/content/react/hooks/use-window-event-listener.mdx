---
title: useWindowEventListener
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_WINDOW_EVENT_LISTENER } from '@/definitions/stack-blitz/react/hooks'

The `useWindowEventListener` hook adds an event listener to the window.
Optionally a dependency list can be passed to refresh the listener.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_WINDOW_EVENT_LISTENER}
  template='react'
  title='useWindowEventListener'
  console
/>
