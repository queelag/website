---
title: useObservableElementComponent
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_OBSERVABLE_ELEMENT_COMPONENT } from '@/definitions/stack-blitz/react/hooks'

The `useObservableElementComponent` hook observes a custom element that extends `BaseElement` by listening to its attribute and state changes.

- Optionally a blacklist of attributes and states can be passed to ignore them.
- Optionally a whitelist of attributes and states can be passed to only observe them.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/react-components': 'latest',
    '@aracna/web': 'latest',
    '@aracna/web-components': 'latest',
    lit: 'latest',
    'lit-html': 'latest'
  }}
  files={SB_USE_OBSERVABLE_ELEMENT_COMPONENT}
  template='vite-react'
  title='useObservableElementComponent'
  console
/>
