---
title: useLifeCycle
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_LIFE_CYCLE } from '@/definitions/stack-blitz/react/hooks'

The `useLifeCycle` hook returns the current life cycle of the component.

- The `CONSTRUCTED` state means that the component has been created but not yet mounted.
- The `MOUNTED` state means that the component has been mounted.
- The `UNMOUNTED` state means that the component has been unmounted.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_LIFE_CYCLE}
  template='react'
  title='useLifeCycle'
  console
/>
