---
title: useDispatch
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_DISPATCH } from '@/definitions/stack-blitz/react/hooks'

The `useDispatch` hook returns a function that can be used to cause a re-render of the component.
Optionally a callback can be passed that will be executed after the dispatch.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_DISPATCH}
  template='react'
  title='useDispatch'
  console
/>
