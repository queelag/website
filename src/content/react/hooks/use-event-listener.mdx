---
title: useEventListener
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_USE_EVENT_LISTENER } from '@/definitions/stack-blitz/react/hooks'

The `useEventListener` hook adds an event listener to the given element.
Optionally a list of dependencies can be passed to refresh the listener.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_EVENT_LISTENER}
  template='react'
  title='useEventListener'
  console
/>
