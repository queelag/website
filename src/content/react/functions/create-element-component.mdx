---
title: createElementComponent
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_CREATE_ELEMENT_COMPONENT } from '@/definitions/stack-blitz/react/functions'

The `createElementComponent` function creates a new React component that renders a custom element.
Optionally the events can be defined as well.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_CREATE_ELEMENT_COMPONENT}
  template='react'
  title='createElementComponent'
/>
