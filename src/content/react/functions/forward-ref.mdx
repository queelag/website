---
title: forwardRef
---

import { CodeBlock } from '@/blocks/CodeBlock'
import { SB_FORWARD_REF } from '@/definitions/stack-blitz/react/functions'

The `forwardRef` function lets your component expose a DOM node to parent component with a ref.
The difference with the original `forwardRef` is that this function has less strict types.

<CodeBlock
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_FORWARD_REF}
  template='react'
  title='forwardRef'
  console={100}
/>
