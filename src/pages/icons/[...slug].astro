---
import Layout from '@/layouts/Layout.astro'
import { getCollection, render, type CollectionEntry } from 'astro:content'

export async function getStaticPaths() {
  let entries: CollectionEntry<'icons'>[] = await getCollection('icons')

  return entries.map((entry: CollectionEntry<'icons'>) => ({ params: { slug: entry.id }, props: { entry } }))
}

const { entry } = Astro.props
const { Content, headings, remarkPluginFrontmatter } = await render(entry)
---

<Layout frontmatter={{ ...remarkPluginFrontmatter, url: entry.id }} headings={headings}>
  <Content />
</Layout>
