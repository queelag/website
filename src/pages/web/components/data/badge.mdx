---
layout: '@/layouts/Layout.astro'
title: Badge
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { BadgeComponentBlock } from '@/blocks/components/data/BadgeComponentBlock.tsx'
import { SB_BADGE } from '@/definitions/stack-blitz/web/components/data'

The **BadgeElement** is a custom web component that represents a badge. A badge in the context of web development and user interfaces is typically a small graphical element used to convey certain information. It's often used to display a count of items or notifications, status information, or to highlight new or unread items.

<BadgeComponentBlock client:load />

<CodeBlock
  client:load
  className='mt-6'
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/web': 'latest',
    '@aracna/web-components': 'latest',
    lit: 'latest',
    'lit-html': 'latest'
  }}
  files={SB_BADGE}
  template='javascript'
  title='Badge'
/>
