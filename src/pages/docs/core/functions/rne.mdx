---
layout: '@/layouts/DocumentationLayout.astro'
title: rne
---

This function returns a new error, "rne" stands for return new error.

### Usage

```ts
import { rne } from '@aracna/core'

// will log an Error instance with "error" message
console.log(rne('error'))
```
