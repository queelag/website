---
layout: '@/layouts/Layout.astro'
title: rv
---

import { StackBlitz } from '@/components/StackBlitz.tsx'
import { SB_RV } from '@/definitions/stack-blitz/core/functions'

The **rv** function is a TypeScript utility function that takes a function as an optional argument, calls it, and returns void. The name **rv** is short for "return void", which reflects the function's purpose.

The **rv** function is a simple but useful utility function that can be used to call a function and return void. By using **rv** instead of manually calling the function and returning void, you can make your code more concise and easier to read.

<StackBlitz
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_RV}
  template='javascript'
  title='rne'
  console={100}
/>
