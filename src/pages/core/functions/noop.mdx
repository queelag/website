---
layout: '@/layouts/Layout.astro'
title: noop
---

import { StackBlitz } from '@/components/StackBlitz.tsx'
import { SB_NOOP } from '@/definitions/stack-blitz/core/functions'

The **noop** function is a simple utility function that takes any number of arguments and returns **undefined**. The name **noop** is short for "no operation", and is commonly used in programming to represent a function that does nothing.

The **noop** function is a simple but useful utility function that can be used in a variety of contexts where a function that does nothing is needed.

<StackBlitz
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_NOOP}
  template='javascript'
  title='noop'
  console={100}
/>
