---
import Layout from '@/layouts/Layout.astro'
import { getCollection, render, type CollectionEntry } from 'astro:content'

export async function getStaticPaths() {
  let entries: CollectionEntry<'core'>[] = await getCollection('core')

  return entries.map((entry: CollectionEntry<'core'>) => ({ params: { slug: entry.id }, props: { entry } }))
}

const { entry } = Astro.props
const { Content, headings, remarkPluginFrontmatter } = await render(entry)
---

<Layout frontmatter={{ ...remarkPluginFrontmatter, url: entry.id }} headings={headings}>
  <Content />
</Layout>
