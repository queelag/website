---
layout: '@/layouts/Layout.astro'
title: VisibilityController
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { SB_VISIBILITY_CONTROLLER } from '@/definitions/stack-blitz/core/modules'

The **VisibilityController** class provides functionality for managing the visibility of elements in an application. It can be useful in a variety of contexts, such as animating the appearance and disappearance of elements or controlling the visibility of UI components based on user input.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_VISIBILITY_CONTROLLER}
  template='javascript'
  title='VisibilityController'
  console={100}
/>
