---
layout: '@/layouts/Layout.astro'
title: Base64
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { SB_BASE64 } from '@/definitions/stack-blitz/core/modules'

The **Base64** class provides static methods for encoding and decoding data using the Base64 encoding scheme. The Base64 encoding scheme represents binary data as a sequence of 64 symbols (A-Z, a-z, 0-9, +, and /).

The **Base64** class is a simple and useful utility class that provides a convenient way to encode and decode binary data using the Base64 encoding scheme.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_BASE64}
  template='javascript'
  title='Base64'
  console={100}
/>
