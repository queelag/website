---
layout: '@/layouts/Layout.astro'
title: Base16
---

import { CodeBlock } from '@/blocks/CodeBlock.tsx'
import { SB_BASE16 } from '@/definitions/stack-blitz/core/modules'

The **Base16** class provides static methods for encoding and decoding data using the Base16 encoding scheme. The Base16 encoding scheme, also known as hexadecimal encoding, represents binary data as a sequence of 16 symbols (0-9 and A-F).

The **Base16** class is a simple and useful utility class that provides a convenient way to encode and decode binary data using the Base16 encoding scheme.

<CodeBlock
  client:load
  dependencies={{ '@aracna/core': 'latest' }}
  files={SB_BASE16}
  template='javascript'
  title='Base16'
  console={100}
/>
