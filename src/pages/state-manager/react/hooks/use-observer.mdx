---
layout: '@/layouts/Layout.astro'
title: useObserver
---

import { StackBlitz } from '@/components/StackBlitz.tsx'
import { SB_USE_OBSERVER_USAGE_FILES } from '@/definitions/stack-blitz/state-manager/react/hooks'

This hook performs a re-render when any of the observerd properties used inside the render function change.

## Usage

<StackBlitz
  client:load
  dependencies={{
    '@aracna/core': 'latest',
    '@aracna/react': 'latest',
    '@aracna/state-manager': 'latest',
    '@aracna/state-manager-react': 'latest',
    '@aracna/web': 'latest'
  }}
  files={SB_USE_OBSERVER_USAGE_FILES}
  template='create-react-app'
  title='useObserver'
/>
